---
title: '<OpenIM SDK for Platform>'
description: '<One sentence describing where this SDK runs and what developers can build with it.>'
product: 'sdk'
context: 'chat/sdk/<platform>'
template: 'overview'
status: 'draft'
lastUpdated: 'YYYY-MM-DD'
version: 'v4'
platform: '<ios|android|flutter|uniapp|wasm|electron|miniprogram|react-native|unity>'
sourcePath: '/sdk/<platform>/overview'
---

## What you can build

Summarize the supported runtime, core messaging capabilities, and the primary use cases for this SDK. Keep this section product-specific rather than a generic OpenIM introduction.

## Install

List the supported package manager, minimum runtime versions, and the canonical install command.

```bash
<install command>
```

## Initialize

Show the minimum verified initialization flow for this platform. Include imports, configuration, login, and cleanup when the SDK requires it.

```ts
// Replace with platform-specific code.
```

## Core workflows

- Connect and authenticate a user.
- Create or join a conversation.
- Send and receive messages.
- Handle reconnect, token refresh, and logout.

## Compatibility

Document the supported OpenIM Server version, SDK version, operating system or framework version, and known limitations.

## Next steps

- [Send your first message](/sdk/<platform>/getting-started/send-first-message)
- [Authentication](/sdk/<platform>/application/authenticating-a-user/authentication)
- [Error codes](/sdk/<platform>/error-codes)
