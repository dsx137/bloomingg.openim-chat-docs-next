---
title: '初始化 SDK'
description: '说明 uni-app core binding 方法 InitSDK 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/uniapp'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'uniapp'
sourcePath: '/docs/chat/sdk/v4/uniapp/application/methods/init-sdk'
---

初始化 SDK 全局实例、日志、长连接管理、本地数据目录和基础资源。应用进程内通常只调用一次。

## 功能介绍

初始化 SDK 全局实例、日志、长连接管理、本地数据目录和基础资源。应用进程内通常只调用一次。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## Core Binding 方法

| 项目     | 说明                                                                       |
| -------- | -------------------------------------------------------------------------- |
| 方法签名 | `InitSDK(listener OnConnListener, operationID string, config string) bool` |
| 所属能力 | SDK 生命周期                                                               |
| 源码位置 | `open_im_sdk/init_login.go`                                                |

## uni-app 调用参考

uni-app SDK v3.8.3 的 JavaScript 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

源码位置：`ios/OpenIMUniPlugin/OpenIMUniPlugin/Classes/OpenIMModule.m`。

```js showLineNumbers
openIM.initSDK(operationID, config);
```

## 输入参数

| 参数名称 | 参数类型 | 是否必填   | 描述                              |
| -------- | -------- | ---------- | --------------------------------- |
| config   | Object   | 按方法原型 | 按 uni-app 原生插件导出方法传入。 |

## 返回结果

同步创建类方法直接返回值；异步方法通过 callback 返回成功或失败。建议为每次调用传入可追踪的 operationID。

## 代码示例

以下示例展示 uni-app SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```js showLineNumbers
const openIM = uni.requireNativePlugin('OpenIMUniPlugin');

openIM.initSDK('operationID', {}, callback);
```

## 相关类型

- [初始化配置](/docs/chat/sdk/v4/uniapp/models/init-config)（`InitConfig`）：初始化 SDK 时传入的基础配置。
- [连接状态监听器](/docs/chat/sdk/v4/uniapp/models/on-conn-listener)（`OnConnListener`）：SDK 连接状态和登录票据状态监听器。

## 相关页面

- [SDK 生命周期](/docs/chat/sdk/v4/uniapp/application/overview)
