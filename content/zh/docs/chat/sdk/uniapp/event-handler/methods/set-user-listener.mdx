---
title: '监听用户事件'
description: '说明 uni-app SDK 监听用户事件 的事件范围、注册时机和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/uniapp'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'uniapp'
sourcePath: '/docs/chat/sdk/v4/uniapp/event-handler/methods/set-user-listener'
---
设置用户事件监听器，用于接收当前用户资料更新、用户在线状态变化和用户命令增删改事件。

## 功能介绍

设置用户事件监听器，用于接收当前用户资料更新、用户在线状态变化和用户命令增删改事件。

`Set*Listener` 会替换当前 SDK 保存的同类监听器；如果多个模块都要消费事件，应在业务层封装统一分发。

## Core Binding 方法

| 项目     | 说明                                       |
| -------- | ------------------------------------------ |
| 方法签名 | `SetUserListener(listener OnUserListener)` |
| 所属能力 | 监听器                                     |
| 源码位置 | `open_im_sdk/listener.go`                  |

## uni-app 调用参考

uni-app SDK v3.8.3 的 JavaScript 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

```js showLineNumbers
openIM.setUserListener(operationID, /* ... */, callback);
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

同步创建类方法直接返回值；异步方法通过 callback 返回成功或失败。建议为每次调用传入可追踪的 operationID。

## 代码示例

以下示例展示 uni-app SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```js showLineNumbers
const openIM = uni.requireNativePlugin('OpenIMUniPlugin');

openIM.setUserListener('operationID', callback);
```

## 相关类型

- [用户信息](/docs/chat/sdk/v4/uniapp/models/user-info)（`UserInfo`）：用户资料对象。
- [用户在线状态](/docs/chat/sdk/v4/uniapp/models/users-online-status)（`UsersOnlineStatus`）：用户在线状态信息。
- [用户状态监听器](/docs/chat/sdk/v4/uniapp/models/on-user-listener)（`OnUserListener`）：当前用户资料与用户在线状态变化监听器。

## 相关页面

- [监听器](/docs/chat/sdk/v4/uniapp/event-handler/overview)
