---
title: '发送非 OSS 消息'
description: '说明 Electron OpenIM core 方法 SendMessageNotOss 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/electron'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'electron'
sourcePath: '/docs/chat/sdk/v4/electron/message/sending-messages/send-message-not-oss'
---

发送不走 SDK OSS 上传链路的消息。适合业务自定义上传、资源 URL 已经准备好或使用 `Create*ByURL` 方法创建的消息。

## 功能介绍

发送不走 SDK OSS 上传链路的消息。适合业务自定义上传、资源 URL 已经准备好或使用 `Create*ByURL` 方法创建的消息。

创建消息类方法同步返回消息 JSON；发送、查询、删除、已读和本地写入方法通过回调返回。消息与会话事件分别通过消息监听器和会话监听器接收。

## 底层能力

| 项目     | 说明                                                                                                                                          |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 方法签名 | `SendMessageNotOss(callback SendMsgCallBack, operationID string, message, recvID, groupID string, offlinePushInfo string, isOnlineOnly bool)` |
| 所属能力 | 会话与消息                                                                                                                                    |
| 源码位置 | `open_im_sdk/conversation_msg.go`                                                                                                             |

## Electron 调用参考

Electron SDK v3.8.3 暴露以下平台方法原型。底层能力表用于说明 OpenIM core 能力和参数映射。

源码位置：`src/core/modules/message.ts`。

```ts showLineNumbers
sendMessageNotOss(params: SendMsgParams, opid = uuidV4())
```

## 输入参数

| 参数名称 | 参数类型      | 是否必填 | 描述                               |
| -------- | ------------- | -------- | ---------------------------------- |
| params   | SendMsgParams | 是       | 按平台类型定义传入。               |
| opid     | As defined    | 否       | 链路追踪 ID；不传时 SDK 自动生成。 |

## 返回结果

返回 `Promise` 或注册回调结果。调用失败时 Promise reject；建议在业务层使用 `try/catch` 或 `.catch()` 处理错误。

## 代码示例

以下示例展示 Electron SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```ts showLineNumbers
import { getWithRenderProcess } from '@openim/electron-client-sdk/lib/render';

const { instance: openIM } = getWithRenderProcess();

const result = await openIM.sendMessageNotOss({});
```

## 相关类型

- [底层消息发送回调](/docs/chat/sdk/v4/electron/models/send-msg-call-back)（`SendMsgCallBack`）：OpenIM core 消息发送回调，Electron 调用层通常映射为 OnMsgSendCallback。
- [离线推送信息](/docs/chat/sdk/v4/electron/models/offline-push-info)（`OfflinePushInfo`）：发送消息时携带的离线推送配置。
- [消息对象](/docs/chat/sdk/v4/electron/models/message)（`Message`）：消息对象，包含消息 ID、发送者、会话、内容元素和状态等信息。
- [消息发送回调](/docs/chat/sdk/v4/electron/models/on-msg-send-callback)（`OnMsgSendCallback`）：发送消息时使用的进度、成功和失败回调。

## 相关页面

- [会话与消息](/docs/chat/sdk/v4/electron/message/overview)
