---
title: '监听批量消息事件'
description: '说明 Electron SDK 监听批量消息事件 的事件范围、注册时机和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/electron'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'electron'
sourcePath: '/docs/chat/sdk/v4/electron/event-handler/methods/set-batch-msg-listener'
---
接收批量新消息或多条消息同步结果。

## 功能介绍

批量消息事件适合处理重连、同步或追赶历史时一次性下发的多条消息。建议按批次更新缓存，减少重复刷新。

## 底层能力

| 项目     | 说明                                               |
| -------- | -------------------------------------------------- |
| 方法签名 | `SetBatchMsgListener(listener OnBatchMsgListener)` |
| 所属能力 | 监听器                                             |
| 源码位置 | `open_im_sdk/listener.go`                          |

## Electron 调用参考

Electron SDK v3.8.3 暴露以下平台方法原型。底层能力表用于说明 OpenIM core 能力和参数映射。

源码位置：`src/render.ts`。

```ts showLineNumbers
on(event: CbEvents, listener: (event) => void)
```

## 输入参数

| 参数名称 | 参数类型 | 是否必填 | 描述                   |
| -------- | -------- | -------- | ---------------------- |
| event    | CbEvents | 是       | 需要订阅的事件名称。   |
| listener | Function | 是       | 事件触发时执行的回调。 |

## 返回结果

返回 `Promise` 或注册回调结果。调用失败时 Promise reject；建议在业务层使用 `try/catch` 或 `.catch()` 处理错误。

## 代码示例

Electron 使用事件订阅方式接收回调。按需要替换事件名和处理函数。

```ts showLineNumbers
import { getWithRenderProcess } from '@openim/electron-client-sdk/lib/render';
import { CbEvents } from '@openim/wasm-client-sdk';

const { instance: openIM } = getWithRenderProcess();

openIM.on(CbEvents.OnRecvNewMessages, (event) => {
  console.log(event);
});
```

## 相关类型

- [批量消息监听器](/docs/chat/sdk/v4/electron/models/on-batch-msg-listener)（`OnBatchMsgListener`）：OpenIM core 批量消息监听器，用于接收多条新消息和离线消息。

## 相关页面

- [监听器](/docs/chat/sdk/v4/electron/event-handler/overview)
