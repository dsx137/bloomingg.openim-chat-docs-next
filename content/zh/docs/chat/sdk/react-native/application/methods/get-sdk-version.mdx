---
title: '获取 SDK 版本'
description: '说明 React Native core binding 方法 GetSdkVersion 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/react-native'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'react-native'
sourcePath: '/docs/chat/sdk/v4/react-native/application/methods/get-sdk-version'
---

获取当前 core SDK 的版本号，适合在启动日志、问题反馈和兼容性判断中记录。

## 功能介绍

获取当前 core SDK 的版本号，适合在启动日志、问题反馈和兼容性判断中记录。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## Core Binding 方法

| 项目     | 说明                        |
| -------- | --------------------------- |
| 方法签名 | `GetSdkVersion() string`    |
| 所属能力 | SDK 生命周期                |
| 源码位置 | `open_im_sdk/init_login.go` |

## React Native 调用参考

React Native SDK v3.8.3 的 TypeScript 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

```ts showLineNumbers
getSdkVersion(/* ... */);
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Promise`。调用失败时 Promise reject，错误会被封装为 OpenIMApiError。

## 代码示例

TODO: 当前 React Native SDK v3.8.3 未提供公开 Java 调用示例。

## 相关页面

- [SDK 生命周期](/docs/chat/sdk/v4/react-native/application/overview)
