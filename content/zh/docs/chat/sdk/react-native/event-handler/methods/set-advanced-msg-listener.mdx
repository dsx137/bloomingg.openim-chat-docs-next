---
title: '监听单条消息事件'
description: '说明 React Native SDK 监听单条消息事件 的事件范围、注册时机和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/react-native'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'react-native'
sourcePath: '/docs/chat/sdk/v4/react-native/event-handler/methods/set-advanced-msg-listener'
---
设置单条消息高级监听器，用于处理新消息、离线消息、已读回执、消息撤回、消息删除和在线消息。

## 功能介绍

设置单条消息高级监听器，用于处理新消息、离线消息、已读回执、消息撤回、消息删除和在线消息。

`Set*Listener` 会替换当前 SDK 保存的同类监听器；如果多个模块都要消费事件，应在业务层封装统一分发。

## Core Binding 方法

| 项目     | 说明                                                     |
| -------- | -------------------------------------------------------- |
| 方法签名 | `SetAdvancedMsgListener(listener OnAdvancedMsgListener)` |
| 所属能力 | 监听器                                                   |
| 源码位置 | `open_im_sdk/listener.go`                                |

## React Native 调用参考

React Native SDK v3.8.3 的 TypeScript 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

```ts showLineNumbers
setAdvancedMsgListener(/* ... */);
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Promise`。调用失败时 Promise reject，错误会被封装为 OpenIMApiError。

## 代码示例

以下示例展示 React Native SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```ts showLineNumbers
import OpenIMSDK from '@openim/rn-client-sdk';

const result = await OpenIMSDK.setAdvancedMsgListener();
```

## 相关类型

- [底层高级消息监听器](/docs/chat/sdk/v4/react-native/models/on-advanced-msg-listener)（`OnAdvancedMsgListener`）：core binding 高级消息监听器，React Native 调用层对应 OnAdvanceMsgListener。
- [单聊已读回执信息](/docs/chat/sdk/v4/react-native/models/c2c-read-receipt-info)（`C2CReadReceiptInfo`）：单聊消息已读回执信息。
- [高级消息监听器](/docs/chat/sdk/v4/react-native/models/on-advance-msg-listener)（`OnAdvanceMsgListener`）：接收新消息、已读回执、撤回和消息扩展事件的监听器。
- [群消息已读回执](/docs/chat/sdk/v4/react-native/models/group-message-receipt)（`GroupMessageReceipt`）：群消息已读回执。
- [消息撤回信息](/docs/chat/sdk/v4/react-native/models/revoked-info)（`RevokedInfo`）：消息撤回事件中的撤回详情。
- [消息对象](/docs/chat/sdk/v4/react-native/models/message)（`Message`）：消息对象，包含消息 ID、发送者、会话、内容元素和状态等信息。

## 相关页面

- [监听器](/docs/chat/sdk/v4/react-native/event-handler/overview)
