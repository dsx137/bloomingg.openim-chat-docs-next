---
title: '获取登录用户 ID'
description: '说明 WASM OpenIM core 方法 GetLoginUserID 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/wasm'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'wasm'
sourcePath: '/docs/chat/sdk/v4/wasm/application/methods/get-login-user-id'
---

获取当前 SDK 实例记录的登录用户 ID，常用于日志、会话归属和账号切换判断。

## 功能介绍

获取当前 SDK 实例记录的登录用户 ID，常用于日志、会话归属和账号切换判断。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## 底层能力

| 项目     | 说明                        |
| -------- | --------------------------- |
| 方法签名 | `GetLoginUserID() string`   |
| 所属能力 | SDK 生命周期                |
| 源码位置 | `open_im_sdk/init_login.go` |

## WASM 调用参考

WASM SDK v3.8.3 暴露以下平台方法原型。底层能力表用于说明 OpenIM core 能力和参数映射。

源码位置：`src/sdk/index.ts`。

```ts showLineNumbers
getLoginUserID((operationID = uuidv4()));
```

## 输入参数

| 参数名称    | 参数类型   | 是否必填 | 描述                               |
| ----------- | ---------- | -------- | ---------------------------------- |
| operationID | As defined | 否       | 链路追踪 ID；不传时 SDK 自动生成。 |

## 返回结果

返回 `Promise` 或注册回调结果。调用失败时 Promise reject；建议在业务层使用 `try/catch` 或 `.catch()` 处理错误。

## 代码示例

以下示例展示 WASM SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```ts showLineNumbers
import { getSDK } from '@openim/wasm-client-sdk';

const openIM = getSDK({
  coreWasmPath: '/openIM.wasm',
  sqlWasmPath: '/sql-wasm.wasm',
});

const result = await openIM.getLoginUserID();
```

## 相关页面

- [SDK 生命周期](/docs/chat/sdk/v4/wasm/application/overview)
