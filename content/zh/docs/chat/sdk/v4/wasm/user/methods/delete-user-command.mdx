---
title: '删除用户命令'
description: '说明 WASM OpenIM core 方法 DeleteUserCommand 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/wasm'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'wasm'
sourcePath: '/docs/chat/sdk/v4/wasm/user/methods/delete-user-command'
---

删除指定类型和 UUID 的用户命令数据，用于撤销收藏、移除用户侧命令或清理本地同步数据。

## 功能介绍

删除指定类型和 UUID 的用户命令数据，用于撤销收藏、移除用户侧命令或清理本地同步数据。

用户资料和在线状态方法通常要求当前用户已登录；在线状态变化通过 `SetUserListener` 设置的监听器接收。

## 底层能力

| 项目     | 说明                                                                            |
| -------- | ------------------------------------------------------------------------------- |
| 方法签名 | `DeleteUserCommand(callback Base, operationID string, Type int32, uuid string)` |
| 所属能力 | 用户                                                                            |
| 源码位置 | `open_im_sdk/user.go`                                                           |

## WASM 调用参考

WASM SDK v3.8.3 当前未暴露 `deleteUserCommand` 这个独立平台方法。此页保留底层能力说明，避免把其他平台或旧封装层的调用方式误写成当前 WASM API。

## 输入参数

当前平台未暴露独立方法，因此没有可调用的平台参数。

## 返回结果

当前平台未暴露独立方法，因此没有平台返回值。

## 代码示例

当前 WASM SDK v3.8.3 未暴露 `deleteUserCommand` 独立方法，暂无平台代码示例。

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/wasm/models/base)（`Base`）：OpenIM core 异步结果回调，WASM 调用层通常映射为 Promise。

## 相关页面

- [用户](/docs/chat/sdk/v4/wasm/user/overview)
