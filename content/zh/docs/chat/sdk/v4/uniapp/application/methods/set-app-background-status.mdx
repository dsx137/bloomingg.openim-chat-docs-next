---
title: '设置前后台状态'
description: '说明 uni-app core binding 方法 SetAppBackgroundStatus 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/uniapp'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'uniapp'
sourcePath: '/docs/chat/sdk/v4/uniapp/application/methods/set-app-background-status'
---

通知 SDK 应用进入前台或后台，便于 SDK 调整连接、同步和推送相关行为。

## 功能介绍

通知 SDK 应用进入前台或后台，便于 SDK 调整连接、同步和推送相关行为。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## Core Binding 方法

| 项目     | 说明                                                                           |
| -------- | ------------------------------------------------------------------------------ |
| 方法签名 | `SetAppBackgroundStatus(callback Base, operationID string, isBackground bool)` |
| 所属能力 | 初始化与登录                                                                   |
| 源码位置 | `open_im_sdk/init_login.go`                                                    |

## uni-app 调用参考

uni-app SDK v3.8.3 的 JavaScript 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

源码位置：`ios/OpenIMUniPlugin/OpenIMUniPlugin/Classes/OpenIMModule.m`。

```js showLineNumbers
openIM.setAppBackgroundStatus(operationID, isBackground, callback);
```

## 输入参数

| 参数名称     | 参数类型 | 是否必填   | 描述                              |
| ------------ | -------- | ---------- | --------------------------------- |
| isBackground | boolean  | 按方法原型 | 按 uni-app 原生插件导出方法传入。 |
| callback     | Function | 是         | 按 uni-app 原生插件导出方法传入。 |

## 返回结果

同步创建类方法直接返回值；异步方法通过 callback 返回成功或失败。建议为每次调用传入可追踪的 operationID。

## 代码示例

以下示例展示 uni-app SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```js showLineNumbers
const openIM = uni.requireNativePlugin('OpenIMUniPlugin');

openIM.setAppBackgroundStatus('operationID', false, callback);
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/uniapp/models/base)（`Base`）：core binding 异步结果回调，uni-app 调用层通常映射为 callback。

## 相关页面

- [初始化与登录](/docs/chat/sdk/v4/uniapp/application/overview)
