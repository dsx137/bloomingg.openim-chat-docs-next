---
title: '获取黑名单列表'
description: '说明 Flutter core binding 方法 GetBlackList 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/user/blacklist/methods/get-black-list'
---

获取当前用户黑名单列表，适合隐私设置页、拦截列表或关系状态恢复。

## 功能介绍

获取当前用户黑名单列表，适合隐私设置页、拦截列表或关系状态恢复。

黑名单新增和移除通过 `SetFriendListener` 设置的 `OnFriendshipListener` 接收；屏蔽后的业务效果以当前服务端策略为准。

## Core Binding 方法

| 项目     | 说明                                              |
| -------- | ------------------------------------------------- |
| 方法签名 | `GetBlackList(callback Base, operationID string)` |
| 所属能力 | 黑名单                                            |
| 源码位置 | `open_im_sdk/relation.go`                         |

## Flutter 调用参考

Flutter SDK 3.8.3+hotfix.12 的 Dart 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

源码位置：`lib/src/manager/im_friendship_manager.dart`。

```dart showLineNumbers
Future<List<BlacklistInfo>> getBlacklist({String? operationID})
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Future<List<BlacklistInfo>>`。调用失败时 Future 抛出异常，建议在业务层捕获并记录 operationID。

## 代码示例

以下示例展示 Flutter SDK 3.8.3+hotfix.12 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```dart showLineNumbers
final result = await OpenIM.iMManager.friendshipManager.getBlacklist();
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/flutter/models/base)（`Base`）：core binding 异步结果回调，Flutter 调用层通常映射为 Future。
- [好友关系监听器](/docs/chat/sdk/v4/flutter/models/on-friendship-listener)（`OnFriendshipListener`）：好友、好友申请和黑名单变化监听器。
- [用户信息](/docs/chat/sdk/v4/flutter/models/user-info)（`UserInfo`）：用户资料对象。

## 相关页面

- [黑名单](/docs/chat/sdk/v4/flutter/user/blacklist/overview)
