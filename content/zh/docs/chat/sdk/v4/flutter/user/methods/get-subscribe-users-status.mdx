---
title: '获取已订阅用户状态'
description: '说明 Flutter core binding 方法 GetSubscribeUsersStatus 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/user/methods/get-subscribe-users-status'
---

获取当前已订阅用户的在线状态快照，适合页面进入时先拉一次状态，再等待事件更新。

## 功能介绍

获取当前已订阅用户的在线状态快照，适合页面进入时先拉一次状态，再等待事件更新。

用户资料和在线状态方法通常要求当前用户已登录；在线状态变化通过 `SetUserListener` 设置的监听器接收。

## Core Binding 方法

| 项目     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 方法签名 | `GetSubscribeUsersStatus(callback Base, operationID string)` |
| 所属能力 | 用户                                                         |
| 源码位置 | `open_im_sdk/online.go`                                      |

## Flutter 调用参考

Flutter SDK v3.8.3 的 Dart 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

源码位置：`lib/src/manager/im_user_manager.dart`。

```dart showLineNumbers
Future<List<UserStatusInfo>> getSubscribeUsersStatus({ String? operationID, })
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Future<List<UserStatusInfo>>`。调用失败时 Future 抛出异常，建议在业务层捕获并记录 operationID。

## 代码示例

以下示例展示 Flutter SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```dart showLineNumbers
final result = await OpenIM.iMManager.userManager.getSubscribeUsersStatus();
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/flutter/models/base)（`Base`）：core binding 异步结果回调，Flutter 调用层通常映射为 Future。
- [用户在线状态](/docs/chat/sdk/v4/flutter/models/users-online-status)（`UsersOnlineStatus`）：用户在线状态信息。

## 相关页面

- [用户](/docs/chat/sdk/v4/flutter/user/overview)
