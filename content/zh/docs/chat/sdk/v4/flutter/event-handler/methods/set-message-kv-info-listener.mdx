---
title: '监听消息 KV 变化'
description: '说明 Flutter SDK 监听消息 KV 变化 的事件范围、注册时机和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/event-handler/methods/set-message-kv-info-listener'
---
设置消息 KV 信息变化监听器，用于接收消息扩展键值信息变化。

## 功能介绍

设置消息 KV 信息变化监听器，用于接收消息扩展键值信息变化。

`Set*Listener` 会替换当前 SDK 保存的同类监听器；如果多个模块都要消费事件，应在业务层封装统一分发。

## Core Binding 方法

| 项目     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 方法签名 | `SetMessageKvInfoListener(listener OnMessageKvInfoListener)` |
| 所属能力 | 监听器                                                       |
| 源码位置 | `open_im_sdk/listener.go`                                    |

## Flutter 调用参考

Flutter SDK v3.8.3 的 Dart 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

```dart showLineNumbers
setMessageKvInfoListener(/* ... */)
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Future`。调用失败时 Future 抛出异常，建议在业务层捕获并记录 operationID。

## 代码示例

以下示例展示 Flutter SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```dart showLineNumbers
final result = await OpenIM.iMManager.messageManager.setMessageKvInfoListener();
```

## 相关类型

- [消息扩展监听器](/docs/chat/sdk/v4/flutter/models/on-message-kv-info-listener)（`OnMessageKvInfoListener`）：消息扩展键值变化监听器。
- [消息扩展键值](/docs/chat/sdk/v4/flutter/models/message-kv)（`MessageKv`）：消息扩展键值信息。

## 相关页面

- [监听器](/docs/chat/sdk/v4/flutter/event-handler/overview)
