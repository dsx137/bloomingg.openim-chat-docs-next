---
title: '监听群组事件'
description: '说明 Flutter core binding 方法 SetGroupListener 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/event-handler/methods/set-group-listener'
---

设置群组事件监听器，用于接收已加入群变化、群资料变化、群成员变化和群申请处理结果。

## 功能介绍

设置群组事件监听器，用于接收已加入群变化、群资料变化、群成员变化和群申请处理结果。

`Set*Listener` 会替换当前 SDK 保存的同类监听器；如果多个模块都要消费事件，应在业务层封装统一分发。

## Core Binding 方法

| 项目     | 说明                                         |
| -------- | -------------------------------------------- |
| 方法签名 | `SetGroupListener(listener OnGroupListener)` |
| 所属能力 | 监听器                                       |
| 源码位置 | `open_im_sdk/listener.go`                    |

## Flutter 调用参考

Flutter SDK 3.8.3+hotfix.12 的 Dart 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

```dart showLineNumbers
setGroupListener(/* ... */)
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Future`。调用失败时 Future 抛出异常，建议在业务层捕获并记录 operationID。

## 代码示例

以下示例展示 Flutter SDK 3.8.3+hotfix.12 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```dart showLineNumbers
final result = await OpenIM.iMManager.groupManager.setGroupListener();
```

## 相关类型

- [群成员信息](/docs/chat/sdk/v4/flutter/models/group-members-info)（`GroupMembersInfo`）：群成员资料，包含群内昵称、角色、入群来源和禁言时间。
- [群组监听器](/docs/chat/sdk/v4/flutter/models/on-group-listener)（`OnGroupListener`）：群组、入群申请和群成员变化监听器。
- [群组信息](/docs/chat/sdk/v4/flutter/models/group-info)（`GroupInfo`）：群组资料对象，包含群名、公告、群主、成员数和入群设置。

## 相关页面

- [监听器](/docs/chat/sdk/v4/flutter/event-handler/overview)
