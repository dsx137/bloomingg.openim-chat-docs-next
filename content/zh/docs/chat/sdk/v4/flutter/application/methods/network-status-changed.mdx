---
title: '通知网络变化'
description: '说明 Flutter core binding 方法 NetworkStatusChanged 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/application/methods/network-status-changed'
---

通知 SDK 网络状态已变化，当前实现会触发长连接关闭并进入后续重连处理。

## 功能介绍

通知 SDK 网络状态已变化，当前实现会触发长连接关闭并进入后续重连处理。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## Core Binding 方法

| 项目     | 说明                                                      |
| -------- | --------------------------------------------------------- |
| 方法签名 | `NetworkStatusChanged(callback Base, operationID string)` |
| 所属能力 | SDK 生命周期                                              |
| 源码位置 | `open_im_sdk/init_login.go`                               |

## Flutter 调用参考

Flutter SDK v3.8.3 的 Dart 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

```dart showLineNumbers
networkStatusChanged(/* ... */)
```

## 输入参数

| 参数名称 | 参数类型   | 是否必填   | 描述                 |
| -------- | ---------- | ---------- | -------------------- |
| 参数     | 见方法原型 | 按方法原型 | 按上方调用原型传入。 |

## 返回结果

返回 `Future`。调用失败时 Future 抛出异常，建议在业务层捕获并记录 operationID。

## 代码示例

以下示例展示 Flutter SDK v3.8.3 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```dart showLineNumbers
final result = await OpenIM.iMManager.userManager.networkStatusChanged();
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/flutter/models/base)（`Base`）：core binding 异步结果回调，Flutter 调用层通常映射为 Future。

## 相关页面

- [SDK 生命周期](/docs/chat/sdk/v4/flutter/application/overview)
