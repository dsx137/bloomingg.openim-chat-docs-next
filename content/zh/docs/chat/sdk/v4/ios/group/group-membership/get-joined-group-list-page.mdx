---
title: '分页获取已加入群组'
description: '说明 iOS core binding 方法 GetJoinedGroupListPage 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/ios'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'ios'
sourcePath: '/docs/chat/sdk/v4/ios/group/group-membership/get-joined-group-list-page'
---

分页获取当前用户已加入群组，适合群数量较多的账号。

## 功能介绍

分页获取当前用户已加入群组，适合群数量较多的账号。

群组方法通常要求当前用户已登录，且群主、管理员、成员权限由服务端校验；群组变化通过 `SetGroupListener` 接收。

## Core Binding 方法

| 项目     | 说明                                                                             |
| -------- | -------------------------------------------------------------------------------- |
| 方法签名 | `GetJoinedGroupListPage(callback Base, operationID string, offset, count int32)` |
| 所属能力 | 群组                                                                             |
| 源码位置 | `open_im_sdk/group.go`                                                           |

## iOS 调用参考

iOS Core SDK 3.8.3-hotfix.14-dynamic.1 的 Objective-C 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

源码位置：`Framework/OpenIMCore.xcframework/.../Open_im_sdk.objc.h`。

```objective-c showLineNumbers
FOUNDATION_EXPORT void Open_im_sdkGetJoinedGroupListPage(id<Open_im_sdk_callbackBase> _Nullable callback, NSString* _Nullable operationID, int32_t offset, int32_t count);
```

## 输入参数

| 参数名称 | 参数类型                     | 是否必填   | 描述           |
| -------- | ---------------------------- | ---------- | -------------- |
| callback | id<Open_im_sdk_callbackBase> | 是         | 回调或监听器。 |
| offset   | int32_t                      | 按方法原型 | 分页参数。     |
| count    | int32_t                      | 按方法原型 | 分页参数。     |

## 返回结果

同步创建类方法直接返回值；异步方法通过 callback 返回成功或失败。建议为每次调用传入可追踪的 operationID。

## 代码示例

以下示例展示 iOS Core SDK 3.8.3-hotfix.14-dynamic.1 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```objective-c showLineNumbers
Open_im_sdkGetJoinedGroupListPage(callback, @"operationID", 0, 0);
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/ios/models/base)（`Base`）：core binding 异步结果回调，iOS 调用层通常映射为 Base。
- [群组信息](/docs/chat/sdk/v4/ios/models/group-info)（`GroupInfo`）：群组资料对象，包含群名、公告、群主、成员数和入群设置。

## 相关页面

- [群组](/docs/chat/sdk/v4/ios/group/overview)
