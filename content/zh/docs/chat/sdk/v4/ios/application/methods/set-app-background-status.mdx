---
title: '设置前后台状态'
description: '说明 iOS core binding 方法 SetAppBackgroundStatus 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/ios'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'ios'
sourcePath: '/docs/chat/sdk/v4/ios/application/methods/set-app-background-status'
---

通知 SDK 应用进入前台或后台，便于 SDK 调整连接、同步和推送相关行为。

## 功能介绍

通知 SDK 应用进入前台或后台，便于 SDK 调整连接、同步和推送相关行为。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## Core Binding 方法

| 项目     | 说明                                                                           |
| -------- | ------------------------------------------------------------------------------ |
| 方法签名 | `SetAppBackgroundStatus(callback Base, operationID string, isBackground bool)` |
| 所属能力 | 初始化与登录                                                                   |
| 源码位置 | `open_im_sdk/init_login.go`                                                    |

## iOS 调用参考

iOS Core SDK 3.8.3-hotfix.14-dynamic.1 的 Objective-C 方法原型如下。Core binding 方法签名用于说明底层能力和参数映射。

源码位置：`Framework/OpenIMCore.xcframework/.../Open_im_sdk.objc.h`。

```objective-c showLineNumbers
FOUNDATION_EXPORT void Open_im_sdkSetAppBackgroundStatus(id<Open_im_sdk_callbackBase> _Nullable callback, NSString* _Nullable operationID, BOOL isBackground);
```

## 输入参数

| 参数名称     | 参数类型                     | 是否必填   | 描述             |
| ------------ | ---------------------------- | ---------- | ---------------- |
| callback     | id<Open_im_sdk_callbackBase> | 是         | 回调或监听器。   |
| isBackground | BOOL                         | 按方法原型 | 按方法原型传入。 |

## 返回结果

同步创建类方法直接返回值；异步方法通过 callback 返回成功或失败。建议为每次调用传入可追踪的 operationID。

## 代码示例

以下示例展示 iOS Core SDK 3.8.3-hotfix.14-dynamic.1 的调用骨架，请按业务上下文补充真实参数、错误处理和 UI 状态更新。

```objective-c showLineNumbers
Open_im_sdkSetAppBackgroundStatus(callback, @"operationID", NO);
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/ios/models/base)（`Base`）：core binding 异步结果回调，iOS 调用层通常映射为 Base。

## 相关页面

- [初始化与登录](/docs/chat/sdk/v4/ios/application/overview)
