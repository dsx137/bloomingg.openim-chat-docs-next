---
title: 'UpdateFcmToken'
description: '说明 Android core binding 方法 UpdateFcmToken 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/push-notifications/methods/update-fcm-token'
---

更新当前设备的 FCM token，用于服务端向 Android 设备发送离线推送。

## 功能介绍

更新当前设备的 FCM token，用于服务端向 Android 设备发送离线推送。

第三方能力至少要求 SDK 已初始化；日志和文件上传的进度通过对应 progress callback 返回。

## Core Binding 方法

| 项目     | 说明                                                                            |
| -------- | ------------------------------------------------------------------------------- |
| 方法签名 | `UpdateFcmToken(callback Base, operationID, fcmToken string, expireTime int64)` |
| 所属能力 | 推送、日志与文件                                                                |
| 源码位置 | `open_im_sdk/third.go`                                                          |

## Android 调用参考

TODO: 补充当前 Android SDK 的函数原型。

## 输入参数

| 参数名称      | 类型     | 描述                       |
| ------------- | -------- | -------------------------- |
| `callback`    | `Base`   | 异步结果回调。             |
| `operationID` | `string` | 本次操作的追踪 ID。        |
| `fcmToken`    | `string` | 按 core binding 签名传入。 |
| `expireTime`  | `int64`  | 按 core binding 签名传入。 |

## 返回结果

通过回调返回结果：成功走 `OnSuccess`，失败走 `OnError`；发送或上传类方法还可能通过进度回调返回进度。

## 代码示例

TODO: 补充该方法的 Android 调用示例。

## 相关页面

- [推送、日志与文件](/docs/chat/sdk/v4/android/push-notifications/overview)
