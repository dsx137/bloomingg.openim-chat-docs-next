---
title: '监听好友事件'
description: '说明 Android core binding 方法 SetFriendListener 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/event-handler/methods/set-friend-listener'
---

设置好友关系监听器，用于接收好友申请、好友新增或删除、好友资料变化以及黑名单变化。

## 功能介绍

设置好友关系监听器，用于接收好友申请、好友新增或删除、好友资料变化以及黑名单变化。

`Set*Listener` 会替换当前 SDK 保存的同类监听器；如果多个模块都要消费事件，应在业务层封装统一分发。

## Core Binding 方法

| 项目     | 说明                                               |
| -------- | -------------------------------------------------- |
| 方法签名 | `SetFriendListener(listener OnFriendshipListener)` |
| 所属能力 | 监听器                                             |
| 源码位置 | `open_im_sdk/listener.go`                          |

## Android 调用参考

Android SDK v3.8.3.5 的 Java 方法原型如下，示例代码也基于该调用方式。Core binding 方法签名用于说明底层 `open_im_sdk` 能力和参数映射。

```java showLineNumbers
public void setOnFriendshipListener(OnFriendshipListener listener)
```

## 输入参数

| 参数名称 | 参数类型                  | 是否必填 | 描述 |
| -------- | ------------------------- | -------- | ---- |
| listener | id <OnFriendshipListener> | 是       |      |

## 返回结果

无

## 代码示例

以下示例展示 Android SDK v3.8.3.5 的调用方式。

```java showLineNumbers
OpenIMClient.getInstance().friendshipManager.setOnFriendshipListener(new OnFriendshipListener() {
    @Override
    public void onFriendAdded(FriendInfo info) {
        // TODO: 处理新增好友
    }

    @Override
    public void onFriendDeleted(FriendInfo info) {
        // TODO: 处理好友删除
    }

    @Override
    public void onBlacklistAdded(BlacklistInfo info) {
        // TODO: 处理加入黑名单
    }
});
```

## 相关页面

- [监听器](/docs/chat/sdk/v4/android/event-handler/overview)
