---
title: '获取 SDK 版本'
description: '说明 Android core binding 方法 GetSdkVersion 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/application/methods/get-sdk-version'
---

获取当前 core SDK 的版本号，适合在启动日志、问题反馈和兼容性判断中记录。

## 功能介绍

获取当前 core SDK 的版本号，适合在启动日志、问题反馈和兼容性判断中记录。

初始化类方法应在应用进程内按需调用；用户态方法通常要求 `InitSDK` 已成功执行，并通过 `operationID` 串联日志。

## Core Binding 方法

| 项目     | 说明                        |
| -------- | --------------------------- |
| 方法签名 | `GetSdkVersion() string`    |
| 所属能力 | 初始化与登录                |
| 源码位置 | `open_im_sdk/init_login.go` |

## Android 调用参考

当前 Android SDK v3.8.3.5 未提供该 core 方法对应的公开 Java 调用方法。Core binding 方法签名仅用于说明底层 `open_im_sdk` 能力。

## 输入参数

无输入参数。

## 返回结果

同步返回 `string`。

## 代码示例

TODO: 当前 Android SDK v3.8.3.5 未提供公开 Java 调用示例。

## 相关页面

- [初始化与登录](/docs/chat/sdk/v4/android/application/overview)
