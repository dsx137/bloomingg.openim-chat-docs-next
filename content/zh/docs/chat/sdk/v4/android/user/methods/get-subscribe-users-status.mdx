---
title: '获取已订阅用户状态'
description: '说明 Android core binding 方法 GetSubscribeUsersStatus 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/user/methods/get-subscribe-users-status'
---

获取当前已订阅用户的在线状态快照，适合页面进入时先拉一次状态，再等待事件更新。

## 功能介绍

获取当前已订阅用户的在线状态快照，适合页面进入时先拉一次状态，再等待事件更新。

用户资料和在线状态方法通常要求当前用户已登录；在线状态变化通过 `SetUserListener` 设置的监听器接收。

## Core Binding 方法

| 项目     | 说明                                                         |
| -------- | ------------------------------------------------------------ |
| 方法签名 | `GetSubscribeUsersStatus(callback Base, operationID string)` |
| 所属能力 | 用户                                                         |
| 源码位置 | `open_im_sdk/online.go`                                      |

## Android 调用参考

Android SDK v3.8.3.5 的 Java 方法原型如下，示例代码也基于该调用方式。Core binding 方法签名用于说明底层 `open_im_sdk` 能力和参数映射。

```java showLineNumbers
public void getSubscribeOnlineUsersStatus(OnBase<List<UsersOnlineStatus>> callBack)
```

## 输入参数

无

## 返回结果

| 参数名称 | 参数类型                                                                                                                                            | 描述         |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| callBack | [`OnBase`](/docs/chat/sdk/v4/android/models/on-base)&lt;List&lt;[`UsersOnlineStatus`](/docs/chat/sdk/v4/android/models/users-online-status)&gt;&gt; | 调用成功回调 |

## 代码示例

以下示例展示 Android SDK v3.8.3.5 的调用方式。

```java showLineNumbers
OpenIMClient.getInstance().userInfoManager.getSubscribeOnlineUsersStatus(new OnBase<List<UsersOnlineStatus>>() {
    @Override
    public void onError(int code, String error) {
        // TODO: 处理错误
    }

    @Override
    public void onSuccess(List<UsersOnlineStatus> data) {
        // TODO: 处理已订阅用户在线状态
    }
});
```

## 相关类型

- [底层通用回调](/docs/chat/sdk/v4/android/models/base)（`Base`）：core binding 异步结果回调，Android 调用层通常映射为 OnBase。
- [通用回调](/docs/chat/sdk/v4/android/models/on-base)（`OnBase`）：Android SDK 异步方法的通用成功和失败回调。
- [用户在线状态](/docs/chat/sdk/v4/android/models/users-online-status)（`UsersOnlineStatus`）：用户在线状态信息。

## 相关页面

- [用户](/docs/chat/sdk/v4/android/user/overview)
