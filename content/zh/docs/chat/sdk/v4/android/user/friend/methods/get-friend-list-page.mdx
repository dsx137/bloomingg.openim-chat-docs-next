---
title: '分页获取好友列表'
description: '说明 Android core binding 方法 GetFriendListPage 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/user/friend/methods/get-friend-list-page'
---

分页获取好友列表，适合好友数量较多时按页加载。

## 功能介绍

分页获取好友列表，适合好友数量较多时按页加载。

好友关系变化通过 `SetFriendListener` 设置的 `OnFriendshipListener` 接收；涉及列表和请求对象的字符串参数应传入对应结构的 JSON。

## Core Binding 方法

| 项目     | 说明                                                                                                |
| -------- | --------------------------------------------------------------------------------------------------- |
| 方法签名 | `GetFriendListPage(callback Base, operationID string, offset int32, count int32, filterBlack bool)` |
| 所属能力 | 好友                                                                                                |
| 源码位置 | `open_im_sdk/relation.go`                                                                           |

## Android 调用参考

Android SDK v3.8.3.5 的 Java 方法原型如下，示例代码也基于该调用方式。Core binding 方法签名用于说明底层 `open_im_sdk` 能力和参数映射。

```java showLineNumbers
public void getFriendListPage(OnBase<List<UserInfo>> base, int offset, int count, Boolean filterBlack)
```

## 输入参数

| 参数名称    | 参数类型 | 是否必填 | 描述             |
| ----------- | -------- | -------- | ---------------- |
| offset      | int      | 是       | 分页拉取起始下标 |
| count       | int      | 是       | 一页拉取的数量   |
| filterBlack | Boolean  | 否       | 是否过滤掉黑名单 |

## 返回结果

| 参数名称 | 参数类型       | 描述     |
| -------- | -------------- | -------- |
| data     | List<UserInfo> | 成功返回 |

## 代码示例

以下示例展示 Android SDK v3.8.3.5 的调用方式。

```java showLineNumbers
OpenIMClient.getInstance().friendshipManager.getFriendListPage(
    new OnBase<List<UserInfo>>() {
    @Override
    public void onError(int code, String error) {
        // TODO: 处理错误
    }

    @Override
    public void onSuccess(List<UserInfo> data) {
        // TODO: 处理分页好友列表
    }
},
    offset,
    count,
    filterBlack
);
```

## 相关页面

- [好友](/docs/chat/sdk/v4/android/user/friend/overview)
