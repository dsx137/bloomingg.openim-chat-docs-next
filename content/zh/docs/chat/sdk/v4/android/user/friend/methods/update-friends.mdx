---
title: '更新好友资料'
description: '说明 Android core binding 方法 UpdateFriends 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/user/friend/methods/update-friends'
---

更新好友关系资料，例如备注、扩展字段或其他好友关系字段。

## 功能介绍

更新好友关系资料，例如备注、扩展字段或其他好友关系字段。

好友关系变化通过 `SetFriendListener` 设置的 `OnFriendshipListener` 接收；涉及列表和请求对象的字符串参数应传入对应结构的 JSON。

## Core Binding 方法

| 项目     | 说明                                                           |
| -------- | -------------------------------------------------------------- |
| 方法签名 | `UpdateFriends(callback Base, operationID string, req string)` |
| 所属能力 | 好友                                                           |
| 源码位置 | `open_im_sdk/relation.go`                                      |

## Android 调用参考

TODO: 补充当前 Android SDK 的函数原型。

## 输入参数

| 参数名称         | 参数类型         | 是否必填 | 描述         |
| ---------------- | ---------------- | -------- | ------------ |
| updateFriendsReq | UpdateFriendsReq | 是       | 变更的结构体 |

## 返回结果

通过回调返回结果：成功走 `OnSuccess`，失败走 `OnError`；发送或上传类方法还可能通过进度回调返回进度。

## 代码示例

以下示例展示当前 Android SDK 的调用方式。

```java showLineNumbers
OpenIMClient.getInstance().friendshipManager.updateFriendsReq(new OnBase<String>() {
    @Override
    public void onError(int code, String error) {
      // todo: 处理错误信息
    }
    @Override
    public void onSuccess(String data) {
      // todo: 请求成功
    }
}, updateFriendsReq);
```

## 相关页面

- [好友](/docs/chat/sdk/v4/android/user/friend/overview)
