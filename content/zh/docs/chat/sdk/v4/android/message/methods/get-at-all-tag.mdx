---
title: '获取 @ 所有人标记'
description: '说明 Android core binding 方法 GetAtAllTag 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/message/methods/get-at-all-tag'
---

获取群聊 @ 所有人使用的特殊标记。发送 @ 所有人消息时，应使用该标记而不是业务侧随意拼接字符串。

## 功能介绍

获取群聊 @ 所有人使用的特殊标记。发送 @ 所有人消息时，应使用该标记而不是业务侧随意拼接字符串。

创建消息类方法同步返回消息 JSON；发送、查询、删除、已读和本地写入方法通过回调返回。消息与会话事件分别通过消息监听器和会话监听器接收。

## Core Binding 方法

| 项目     | 说明                                     |
| -------- | ---------------------------------------- |
| 方法签名 | `GetAtAllTag(operationID string) string` |
| 所属能力 | 会话与消息                               |
| 源码位置 | `open_im_sdk/conversation_msg.go`        |

## Android 调用参考

Android SDK v3.8.3.5 的 Java 方法原型如下，示例代码也基于该调用方式。Core binding 方法签名用于说明底层 `open_im_sdk` 能力和参数映射。

```java showLineNumbers
public String getAtAllTag()
```

## 输入参数

| 参数名称      | 类型     | 描述                |
| ------------- | -------- | ------------------- |
| `operationID` | `string` | 本次操作的追踪 ID。 |

## 返回结果

同步返回 `string`。

## 代码示例

以下示例展示 Android SDK v3.8.3.5 的调用方式。

```java showLineNumbers
String atAllTag = OpenIMClient.getInstance().conversationManager.getAtAllTag();
```

## 相关页面

- [会话与消息](/docs/chat/sdk/v4/android/message/overview)
