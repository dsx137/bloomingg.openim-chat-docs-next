---
title: '更新群资料'
description: '说明 Android core binding 方法 SetGroupInfo 的用途、参数、返回和使用边界。'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/channel/methods/set-group-info'
---

更新群资料，例如群名称、头像、公告或扩展字段。

## 功能介绍

更新群资料，例如群名称、头像、公告或扩展字段。

群组方法通常要求当前用户已登录，且群主、管理员、成员权限由服务端校验；群组变化通过 `SetGroupListener` 接收。

## Core Binding 方法

| 项目     | 说明                                                                |
| -------- | ------------------------------------------------------------------- |
| 方法签名 | `SetGroupInfo(callback Base, operationID string, groupInfo string)` |
| 所属能力 | 群组                                                                |
| 源码位置 | `open_im_sdk/group.go`                                              |

## Android 调用参考

当前 Android SDK 的方法原型如下，示例代码也基于该调用方式。Core binding 方法签名用于说明底层 `open_im_sdk` 能力和参数映射。

```java showLineNumbers
public void setGroupInfo(GroupInfo groupInfo, OnBase<String> callBack)
```

## 输入参数

| 参数名称  | 参数类型  | 是否必填 | 描述     |
| --------- | --------- | -------- | -------- |
| callBack  | OnBase    | 是       | 回调接口 |
| groupInfo | GroupInfo | 是       | 群 信息  |

## 返回结果

通过回调返回结果：成功走 `OnSuccess`，失败走 `OnError`；发送或上传类方法还可能通过进度回调返回进度。

## 代码示例

以下示例展示当前 Android SDK 的调用方式。

```java showLineNumbers
OpenIMClient.getInstance().groupManager.setGroupInfo(groupInfo, new OnBase<String>() {
    @Override
    public void onError(int code, String error) {
      // todo: 处理错误信息
    }
    @Override
    public void onSuccess(String data) {
      // todo: 请求成功
    }
});
```

## 相关页面

- [群组](/docs/chat/sdk/v4/android/channel/overview)
