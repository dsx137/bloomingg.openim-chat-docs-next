---
title: "Initiate multipart upload"
description: "Initiate multipart upload with the OpenIM Platform API, including endpoint, request body, response fields, and error handling."
product: "platform-api"
context: "chat/platform-api/v3"
template: "api"
status: "published"
lastUpdated: "2026-07-07"
version: "v3"
sourcePath: "/docs/chat/platform-api/v3/third/object-storage/initiate-multipart-upload"
---

Initiate multipart upload is an OpenIM third-party service REST API for trusted backend services. Configure the API address and administrator token in [Prepare to use the API](/docs/chat/platform-api/v3/prepare-to-use-api) before calling management endpoints.

## HTTP request

```bash
POST {API_ADDRESS}/object/initiate_multipart_upload
```

### Request example

```bash
curl --request POST "${API_ADDRESS}/object/initiate_multipart_upload" \
  --header "Content-Type: application/json; charset=utf-8" \
  --header "operationID: ${OPERATION_ID}" \
  --header "token: ${ADMIN_TOKEN}" \
  --data-raw '{
  "hash": "sha256_file_hash",
  "size": 10485760,
  "partSize": 5242880,
  "maxParts": 2,
  "cause": "message",
  "name": "attachments/file.zip",
  "contentType": "application/zip"
}'
```

> Keep administrator tokens on trusted backend services only. Client applications should use user tokens issued by your backend.

## Request body

```json
{
  "hash": "sha256_file_hash",
  "size": 10485760,
  "partSize": 5242880,
  "maxParts": 2,
  "cause": "message",
  "name": "attachments/file.zip",
  "contentType": "application/zip"
}
```

| Parameter | Required | Type | Description |
| --- | --- | --- | --- |
| hash | Yes | string | The hash value. |
| size | Yes | int | The size value. |
| partSize | Yes | int | Multipart upload part size. |
| maxParts | Yes | int | The max parts value. |
| cause | No | string | The cause value. |
| name | Yes | string | Resource name. |
| contentType | No | string | OpenIM message content type. |
| urlPrefix | No | string | The url prefix value. |

## Response

OpenIM usually returns `200 OK` when the request reaches the service. Use `errCode` in the JSON response to determine business success; `errCode === 0` means the operation succeeded.

```json
{
  "errCode": 0,
  "errMsg": "",
  "errDlt": "",
  "data": {
    "url": "",
    "upload": {
      "uploadID": "upload_id_sample",
      "partSize": 5242880,
      "sign": {
        "url": "https://storage.example.com/upload",
        "query": [],
        "header": [
          {
            "key": "Content-Type",
            "values": [
              "application/octet-stream"
            ]
          }
        ],
        "parts": [
          {
            "partNumber": 1,
            "url": "https://storage.example.com/upload?partNumber=1",
            "query": [
              {
                "key": "partNumber",
                "values": [
                  "1"
                ]
              }
            ],
            "header": [
              {
                "key": "Content-Type",
                "values": [
                  "application/octet-stream"
                ]
              }
            ]
          }
        ]
      },
      "expireTime": 1719803600000
    }
  }
}
```

### Response fields

| Field | Type | Description |
| --- | --- | --- |
| errCode | int | Business error code. `0` means success. |
| errMsg | string | Short error message. |
| errDlt | string | Detailed error information for troubleshooting. |
| data | object | Endpoint-specific response data. |
| data.url | string | Resource URL. |
| data.upload | object | The upload value. |
| data.upload.uploadID | string | Multipart upload ID. |
| data.upload.partSize | int | Multipart upload part size. |
| data.upload.sign | object | The sign value. |
| data.upload.sign.query | array | The query value. |
| data.upload.sign.query[].key | string | The key value. |
| data.upload.sign.query[].values | array | The values value. |
| data.upload.sign.header | array | The header value. |
| data.upload.sign.header[].key | string | The key value. |
| data.upload.sign.header[].values | array | The values value. |
| data.upload.sign.parts | array | The parts value. |
| data.upload.sign.parts[].partNumber | int | The part number value. |
| data.upload.sign.parts[].url | string | Resource URL. |
| data.upload.sign.parts[].query | array | The query value. |
| data.upload.sign.parts[].query[].key | string | The key value. |
| data.upload.sign.parts[].query[].values | array | The values value. |
| data.upload.sign.parts[].header | array | The header value. |
| data.upload.sign.parts[].header[].key | string | The key value. |
| data.upload.sign.parts[].header[].values | array | The values value. |
| data.upload.expireTime | int | The expire time value. |

### Error response

When a request fails, OpenIM returns the same error envelope. See [Error codes](/docs/chat/platform-api/v3/error-codes) for the full handling model.

```json
{
  "errCode": 1004,
  "errMsg": "RecordNotFoundError",
  "errDlt": ": [1004]RecordNotFoundError"
}
```

| Scenario | Possible cause | Recommended action |
| --- | --- | --- |
| Authentication failed | `token` is missing, expired, or not an administrator token. | Issue a new administrator token and keep it on the backend. |
| Traceability is weak | `operationID` is missing or reused across many requests. | Generate a unique `operationID` for every request and log it with the response. |
| Validation failed | The request body has an invalid type, missing field, or unsupported enum value. | Compare the payload with the request table and retry after correcting the fields. |

## Permissions and limits

- Call this endpoint from a trusted backend service.
- Log `operationID`, the endpoint path, and the response error fields for troubleshooting.
- Treat write operations as state-changing. If a retry follows an uncertain failure, check the resource state first.

## Related pages

- [Prepare to use the API](/docs/chat/platform-api/v3/prepare-to-use-api)
- [third-party service overview](/docs/chat/platform-api/v3/third/overview)
- [Error codes](/docs/chat/platform-api/v3/error-codes)
