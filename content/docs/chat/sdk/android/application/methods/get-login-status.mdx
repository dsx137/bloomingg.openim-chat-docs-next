---
title: 'Get login status'
description: 'Explains the Android core binding method GetLoginStatus, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/application/methods/get-login-status'
---

Use `GetLoginStatus` for get login status in the OpenIM Android SDK.

## Feature

Reads get login status from the local SDK state or OpenIM service according to the method contract.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                              |
| ---------------- | ---------------------------------------- |
| Method signature | `GetLoginStatus(operationID string) int` |
| Capability       | SDK lifecycle                            |
| Source file      | `open_im_sdk/init_login.go`              |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public int getLoginStatus()
```

## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

| Name | Type | Description                           |
| ---- | ---- | ------------------------------------- |
| ~    | int  | Returned when the operation succeeds. |

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
int loginStatus = OpenIMClient.getInstance().getLoginStatus();
```

## Related Types

- [Login Status](/docs/chat/sdk/v4/android/models/login-status) (`LoginStatus`): LoginStatus used by this Android SDK type.
