---
title: 'Set group listener'
description: 'Explains the Android core binding method SetGroupListener, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/event-handler/methods/set-group-listener'
---

Use `SetGroupListener` for set group listener in the OpenIM Android SDK.

## Feature

Registers or replaces the listener used to receive set group listener. Register listeners before login when you need login-time synchronization events.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                  |
| ---------------- | -------------------------------------------- |
| Method signature | `SetGroupListener(listener OnGroupListener)` |
| Capability       | Event listeners                              |
| Source file      | `open_im_sdk/listener.go`                    |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void setOnGroupListener(OnGroupListener listener)
```

## Input Parameters

| Parameter  | Type                                                                    | Description         |
| ---------- | ----------------------------------------------------------------------- | ------------------- |
| `listener` | [On Group Listener](/docs/chat/sdk/v4/android/models/on-group-listener) | Callback Interfaces |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().groupManager.setOnGroupListener(new OnGroupListener() {
    @Override
    public void onJoinedGroupAdded(GroupInfo info) {
        // TODO: handle the callback result
    }

    @Override
    public void onGroupInfoChanged(GroupInfo info) {
        // TODO: handle the callback result
    }

    @Override
    public void onGroupMemberAdded(GroupMembersInfo info) {
        // TODO: handle the callback result
    }
});
```

## Related Types

- [Group Members Info](/docs/chat/sdk/v4/android/models/group-members-info) (`GroupMembersInfo`): GroupMembersInfo used by this Android SDK type.
- [On Group Listener](/docs/chat/sdk/v4/android/models/on-group-listener) (`OnGroupListener`): OnGroupListener used by this Android SDK type.
- [Group Info](/docs/chat/sdk/v4/android/models/group-info) (`GroupInfo`): GroupInfo used by this Android SDK type.
