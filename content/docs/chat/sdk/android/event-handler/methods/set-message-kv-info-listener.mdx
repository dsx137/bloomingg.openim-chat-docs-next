---
title: 'Set message KV info listener'
description: 'Explains the Android core binding method SetMessageKvInfoListener, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/event-handler/methods/set-message-kv-info-listener'
---

Use `SetMessageKvInfoListener` for set message kv info listener in the OpenIM Android SDK.

## Feature

Registers or replaces the listener used to receive set message kv info listener. Register listeners before login when you need login-time synchronization events.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                  |
| ---------------- | ------------------------------------------------------------ |
| Method signature | `SetMessageKvInfoListener(listener OnMessageKvInfoListener)` |
| Capability       | Event listeners                                              |
| Source file      | `open_im_sdk/listener.go`                                    |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void setMessageKvInfoListener(OnMessageKvInfoListener listener)
```

## Input Parameters

| Parameter  | Type                                                                                       | Description              |
| ---------- | ------------------------------------------------------------------------------------------ | ------------------------ |
| `listener` | [On Message KVInfo Listener](/docs/chat/sdk/v4/android/models/on-message-kv-info-listener) | Event listener instance. |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().messageManager.setMessageKvInfoListener(new OnMessageKvInfoListener() {
    @Override
    public void onMessageKvInfoChanged(List<MessageKv> list) {
        // TODO: handle the callback result
    }
});
```

## Related Types

- [On Message KVInfo Listener](/docs/chat/sdk/v4/android/models/on-message-kv-info-listener) (`OnMessageKvInfoListener`): OnMessageKvInfoListener used by this Android SDK type.
- [Message KV](/docs/chat/sdk/v4/android/models/message-kv) (`MessageKv`): MessageKv used by this Android SDK type.
