---
title: 'Get SDK Version'
description: 'Explains the Get SDK Version method in iOS Core SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/ios'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'ios'
sourcePath: '/docs/chat/sdk/v4/ios/application/methods/get-sdk-version'
---

Use **Get SDK Version** when your application needs this OpenIM capability from iOS Core SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `GetSdkVersion` through the platform wrapper to get sdk version. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                 |
| ---------------- | --------------------------- |
| Method signature | `GetSdkVersion() string`    |
| Capability       | SDK lifecycle    |
| Source location  | `open_im_sdk/init_login.go` |

## iOS Call Reference

iOS Core SDK v3.8.3 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`Framework/OpenIMCore.xcframework/.../Open_im_sdk.objc.h`.

```objective-c showLineNumbers
FOUNDATION_EXPORT NSString* _Nonnull Open_im_sdkGetSdkVersion(void);
```

## Parameters

| Parameter | Type                 | Required   | Description                                 |
| --------- | -------------------- | ---------- | ------------------------------------------- |
| Parameter | See method signature | As defined | Pass according to the call signature above. |

## Return Value

Synchronous creation methods return a value directly. Asynchronous methods report success or failure through the callback. Pass a traceable operationID for each call.

## Code Example

Add a platform-specific example based on the method signature above.

## Related types

- See the surrounding SDK section for related pages.

## Related pages

- [SDK lifecycle](/docs/chat/sdk/v4/ios/application/overview)
