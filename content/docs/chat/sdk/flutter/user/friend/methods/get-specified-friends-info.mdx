---
title: 'Get specified friends info'
description: 'Explains the Get specified friends info method in Flutter SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/user/friend/methods/get-specified-friends-info'
---

Use **Get specified friends info** when your application needs this OpenIM capability from Flutter SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `GetSpecifiedFriendsInfo` through the platform wrapper to get specified friends info. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                                                       |
| ---------------- | ------------------------------------------------------------------------------------------------- |
| Method signature | `GetSpecifiedFriendsInfo(callback Base, operationID string, userIDList string, filterBlack bool)` |
| Capability       | Friends                                                                                           |
| Source location  | `open_im_sdk/relation.go`                                                                         |

## Flutter Call Reference

Flutter SDK v3.8.3 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

```dart showLineNumbers
getSpecifiedFriendsInfo(/* ... */)
```

## Parameters

| Parameter | Type                 | Required   | Description                                 |
| --------- | -------------------- | ---------- | ------------------------------------------- |
| Parameter | See method signature | As defined | Pass according to the call signature above. |

## Return Value

Returns `Future`. If the call fails, the Future throws an exception. Catch it in the application layer and keep the operationID in logs.

## Code Example

The following example shows the Flutter SDK v3.8.3 call shape. Replace placeholder values with real business data and add production error handling.

```dart showLineNumbers
final result = await OpenIM.iMManager.friendshipManager.getSpecifiedFriendsInfo();
```

## Related types

- [Base callback](/docs/chat/sdk/v4/flutter/models/base) (`Base`)
- [Friendship listener](/docs/chat/sdk/v4/flutter/models/on-friendship-listener) (`OnFriendshipListener`)
- [User info](/docs/chat/sdk/v4/flutter/models/user-info) (`UserInfo`)

## Related pages

- [Friends](/docs/chat/sdk/v4/flutter/user/overview#friends)
