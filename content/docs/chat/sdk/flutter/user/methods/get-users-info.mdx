---
title: 'Get Users Info'
description: 'Explains the Get Users Info method in Flutter SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/user/methods/get-users-info'
---

Use **Get Users Info** when your application needs this OpenIM capability from Flutter SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `GetUsersInfo` through the platform wrapper to get users info. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                       |
| ---------------- | ----------------------------------------------------------------- |
| Method signature | `GetUsersInfo(callback Base, operationID string, userIDs string)` |
| Capability       | User                                                              |
| Source location  | `open_im_sdk/user.go`                                             |

## Flutter Call Reference

Flutter SDK v3.8.3 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`lib/src/manager/im_user_manager.dart`.

```dart showLineNumbers
Future<List<PublicUserInfo>> getUsersInfo({ required List<String> userIDList, String? operationID, })
```

## Parameters

| Parameter  | Type               | Required | Description                  |
| ---------- | ------------------ | -------- | ---------------------------- |
| userIDList | `List<String>` | Yes      | User ID or list of user IDs. |

## Return Value

Returns `Future<List<PublicUserInfo>>`. If the call fails, the Future throws an exception. Catch it in the application layer and keep the operationID in logs.

## Code Example

The following example shows the Flutter SDK v3.8.3 call shape. Replace placeholder values with real business data and add production error handling.

```dart showLineNumbers
final result = await OpenIM.iMManager.userManager.getUsersInfo(
  userIDList: [],
);
```

## Related types

- [Base callback](/docs/chat/sdk/v4/flutter/models/base) (`Base`)
- [Public user info](/docs/chat/sdk/v4/flutter/models/public-user-info) (`PublicUserInfo`)

## Related pages

- [User](/docs/chat/sdk/v4/flutter/user/overview)
