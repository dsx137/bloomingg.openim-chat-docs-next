---
title: 'Create a group'
description: 'Explains the Create a group method in React Native SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/react-native'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'react-native'
sourcePath: '/docs/chat/sdk/v4/react-native/group/managing-groups/create-group'
---

Use **Create a group** when your application needs this OpenIM capability from React Native SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `CreateGroup` through the platform wrapper to create a group. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                           |
| ---------------- | --------------------------------------------------------------------- |
| Method signature | `CreateGroup(callback Base, operationID string, groupReqInfo string)` |
| Capability       | Group                                                                 |
| Source location  | `open_im_sdk/group.go`                                                |

## React Native Call Reference

React Native SDK v3.8.3 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`src/sdk.ts`.

```ts showLineNumbers
createGroup(params: CreateGroupParams, operationID: string = id())
```

## Parameters

| Parameter | Type              | Required | Description                                                  |
| --------- | ----------------- | -------- | ------------------------------------------------------------ |
| params    | CreateGroupParams | Yes      | Request object. Use the fields defined by the platform type. |

## Return Value

Returns `Promise`. If the call fails, the Promise rejects and the error is wrapped as OpenIMApiError.

## Code Example

The following example shows the React Native SDK v3.8.3 call shape. Replace placeholder values with real business data and add production error handling.

```ts showLineNumbers
import OpenIMSDK from '@openim/rn-client-sdk';

const result = await OpenIMSDK.createGroup({});
```

## Related types

- [Base callback](/docs/chat/sdk/v4/react-native/models/base) (`Base`)
- [Group info](/docs/chat/sdk/v4/react-native/models/group-info) (`GroupInfo`)

## Related pages

- [Group](/docs/chat/sdk/v4/react-native/group/overview)
