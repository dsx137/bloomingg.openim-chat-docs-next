---
title: 'Create an advanced quote message'
description: 'Explains the Create an advanced quote message method in uni-app SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/uniapp'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'uniapp'
sourcePath: '/docs/chat/sdk/v4/uniapp/message/creating-messages/create-advanced-quote-message'
---

Use **Create an advanced quote message** when your application needs this OpenIM capability from uni-app SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `CreateAdvancedQuoteMessage` through the platform wrapper to create an advanced quote message. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                                                             |
| ---------------- | ------------------------------------------------------------------------------------------------------- |
| Method signature | `CreateAdvancedQuoteMessage(operationID string, text string, message, messageEntityList string) string` |
| Capability       | Conversations and messages                                                                              |
| Source location  | `open_im_sdk/conversation_msg.go`                                                                       |

## uni-app Call Reference

uni-app SDK v3.8.3 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`ios/OpenIMUniPlugin/OpenIMUniPlugin/Classes/OpenIMModule.m`.

```js showLineNumbers
openIM.createAdvancedQuoteMessage(operationID, options);
```

## Parameters

| Parameter | Type   | Required   | Description                                         |
| --------- | ------ | ---------- | --------------------------------------------------- |
| options   | Object | As defined | Pass according to the uni-app native plugin export. |

## Return Value

Synchronous creation methods return a value directly. Asynchronous methods report success or failure through the callback. Pass a traceable operationID for each call.

## Code Example

The following example shows the uni-app SDK v3.8.3 call shape. Replace placeholder values with real business data and add production error handling.

```js showLineNumbers
const openIM = uni.requireNativePlugin('OpenIMUniPlugin');

openIM.createAdvancedQuoteMessage('operationID', {}, callback);
```

## Related types

- [RichMessage](/docs/chat/sdk/v4/uniapp/models/rich-message) (`RichMessage`)
- [Message](/docs/chat/sdk/v4/uniapp/models/message) (`Message`)

## Related pages

- [Conversations and messages](/docs/chat/sdk/v4/uniapp/message/overview)
