---
title: 'Get login user ID'
description: 'Explains the Get login user ID method in React Native SDK v3.8.3-patch.12.4, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/react-native'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'react-native'
sourcePath: '/docs/chat/sdk/v4/react-native/application/methods/get-login-user-id'
---

Use **Get login user ID** when your application needs this OpenIM capability from React Native SDK v3.8.3-patch.12.4. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `GetLoginUserID` through the platform wrapper to get login user id. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                 |
| ---------------- | --------------------------- |
| Method signature | `GetLoginUserID() string`   |
| Capability       | Initialization and login    |
| Source location  | `open_im_sdk/init_login.go` |

## React Native Call Reference

React Native SDK v3.8.3-patch.12.4 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`src/sdk.ts`.

```ts showLineNumbers
getLoginUserID(operationID: string = id())
```

## Parameters

| Parameter | Type                 | Required   | Description                                 |
| --------- | -------------------- | ---------- | ------------------------------------------- |
| Parameter | See method signature | As defined | Pass according to the call signature above. |

## Return Value

Returns `Promise`. If the call fails, the Promise rejects and the error is wrapped as OpenIMApiError.

## Code Example

The following example shows the React Native SDK v3.8.3-patch.12.4 call shape. Replace placeholder values with real business data and add production error handling.

```java showLineNumbers
String loginUserID = OpenIMClient.getInstance().getLoginUserID();
```

## Related types

- See the surrounding SDK section for related pages.

## Related pages

- [Initialization and login](/docs/chat/sdk/v4/react-native/application/overview)
