---
title: 'Get friend list'
description: 'Explains the Get friend list method in React Native SDK v3.8.3-patch.12.4, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/react-native'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'react-native'
sourcePath: '/docs/chat/sdk/v4/react-native/user/friend/methods/get-friend-list'
---

Use **Get friend list** when your application needs this OpenIM capability from React Native SDK v3.8.3-patch.12.4. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `GetFriendList` through the platform wrapper to get friend list. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                          |
| ---------------- | -------------------------------------------------------------------- |
| Method signature | `GetFriendList(callback Base, operationID string, filterBlack bool)` |
| Capability       | Friends                                                              |
| Source location  | `open_im_sdk/relation.go`                                            |

## React Native Call Reference

React Native SDK v3.8.3-patch.12.4 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`src/sdk.ts`.

```ts showLineNumbers
getFriendList(filterBlack: boolean, operationID: string = id())
```

## Parameters

| Parameter   | Type    | Required | Description      |
| ----------- | ------- | -------- | ---------------- |
| filterBlack | boolean | Yes      | Pass as defined. |

## Return Value

Returns `Promise`. If the call fails, the Promise rejects and the error is wrapped as OpenIMApiError.

## Code Example

The following example shows the React Native SDK v3.8.3-patch.12.4 call shape. Replace placeholder values with real business data and add production error handling.

```ts showLineNumbers
import OpenIMSDK from '@openim/rn-client-sdk';

const result = await OpenIMSDK.getFriendList(false);
```

## Related types

- [Base callback](/docs/chat/sdk/v4/react-native/models/base) (`Base`)
- [Friendship listener](/docs/chat/sdk/v4/react-native/models/on-friendship-listener) (`OnFriendshipListener`)
- [User info](/docs/chat/sdk/v4/react-native/models/user-info) (`UserInfo`)

## Related pages

- [Friends](/docs/chat/sdk/v4/react-native/user/friend/overview)
