---
title: 'Write client logs'
description: 'Explains the Write client logs method in Electron SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/electron'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'electron'
sourcePath: '/docs/chat/sdk/v4/electron/logger/methods/logs'
---

Use **Write client logs** when your application needs this OpenIM capability from Electron SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying OpenIM core.

## Overview

Call `Logs` through the platform wrapper to write client logs. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Underlying Capability

| Item             | Description                                                                                                                 |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Method signature | `Logs(callback Base, operationID string, logLevel int, file string, line int, msgs string, err string, keyAndValue string)` |
| Capability       | Logger |
| Source location  | `open_im_sdk/third.go`                                                                                                      |

## Electron Call Reference

Electron SDK v3.8.3 does not expose an independent `logs` platform method. This page keeps the underlying capability notes without presenting another platform wrapper as the current Electron API.

## Parameters

No platform parameters are available because this SDK does not expose an independent method.

## Return Value

No platform return value is available because this SDK does not expose an independent method.

## Code Example

Electron SDK v3.8.3 does not expose an independent `logs` method, so there is no platform code example for this page.

## Related types

- [Base callback](/docs/chat/sdk/v4/electron/models/base) (`Base`)
- [LogLevel](/docs/chat/sdk/v4/electron/models/log-level) (`LogLevel`)

## Related pages

- [Logger](/docs/chat/sdk/v4/electron/logger/overview)
