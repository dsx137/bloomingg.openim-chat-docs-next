---
title: 'Get subscribe users status'
description: 'Explains the Android core binding method GetSubscribeUsersStatus, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/user/methods/get-subscribe-users-status'
---

Use `GetSubscribeUsersStatus` for get subscribe users status in the OpenIM Android SDK.

## Feature

Reads get subscribe users status from the local SDK state or OpenIM service according to the method contract.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                  |
| ---------------- | ------------------------------------------------------------ |
| Method signature | `GetSubscribeUsersStatus(callback Base, operationID string)` |
| Capability       | Users, friends, and blocklist                                |
| Source file      | `open_im_sdk/online.go`                                      |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void getSubscribeOnlineUsersStatus(OnBase<List<UsersOnlineStatus>> callBack)
```

## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

| Name       | Type                              | Description                           |
| ---------- | --------------------------------- | ------------------------------------- |
| `callBack` | `OnBase<List<UsersOnlineStatus>>` | Returned when the operation succeeds. |

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().userInfoManager.getSubscribeOnlineUsersStatus(new OnBase<List<UsersOnlineStatus>>() {
    @Override
    public void onError(int code, String error) {
        // TODO: handle the callback result
    }

    @Override
    public void onSuccess(List<UsersOnlineStatus> data) {
        // TODO: handle the callback result
    }
});
```

## Related Types

- [Base](/docs/chat/sdk/v4/android/models/base) (`Base`): Base used by this Android SDK type.
- [On Base](/docs/chat/sdk/v4/android/models/on-base) (`OnBase`): OnBase used by this Android SDK type.
- [Users Online Status](/docs/chat/sdk/v4/android/models/users-online-status) (`UsersOnlineStatus`): UsersOnlineStatus used by this Android SDK type.
