---
title: 'Delete user command'
description: 'Explains the Android core binding method DeleteUserCommand, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/user/methods/delete-user-command'
---

Use `DeleteUserCommand` for delete user command in the OpenIM Android SDK.

## Feature

Removes or clears the target resource for delete user command. Check the current resource state before retrying after an uncertain failure.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                                     |
| ---------------- | ------------------------------------------------------------------------------- |
| Method signature | `DeleteUserCommand(callback Base, operationID string, Type int32, uuid string)` |
| Capability       | Users, friends, and blocklist                                                   |
| Source file      | `open_im_sdk/user.go`                                                           |

## Input Parameters

| Parameter     | Type                                          | Description                                     |
| ------------- | --------------------------------------------- | ----------------------------------------------- |
| `callback`    | [Base](/docs/chat/sdk/v4/android/models/base) | Async callback for success and failure results. |
| `operationID` | `string`                                      | Trace ID for this operation.                    |
| `Type`        | `int32`                                       | Pass according to the core binding signature.   |
| `uuid`        | `string`                                      | Pass according to the core binding signature.   |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Related Types

- [Base](/docs/chat/sdk/v4/android/models/base) (`Base`): Base used by this Android SDK type.
