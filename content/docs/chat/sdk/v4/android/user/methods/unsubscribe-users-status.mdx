---
title: 'Unsubscribe users status'
description: 'Explains the Android core binding method UnsubscribeUsersStatus, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/user/methods/unsubscribe-users-status'
---

Use `UnsubscribeUsersStatus` for unsubscribe users status in the OpenIM Android SDK.

## Feature

UnsubscribeUsersStatus provides the Android SDK operation for unsubscribe users status.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                                 |
| ---------------- | --------------------------------------------------------------------------- |
| Method signature | `UnsubscribeUsersStatus(callback Base, operationID string, userIDs string)` |
| Capability       | Users, friends, and blocklist                                               |
| Source file      | `open_im_sdk/online.go`                                                     |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void unsubscribeOnlineUsersStatus(OnBase<String> callBack, List<String> uid)
```

## Input Parameters

| Parameter  | Type             | Description                                     |
| ---------- | ---------------- | ----------------------------------------------- |
| `callBack` | `OnBase<String>` | Async callback for success and failure results. |
| `uid`      | `List<String>`   | User ID.                                        |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().userInfoManager.unsubscribeOnlineUsersStatus(new OnBase<String>() {
    @Override
    public void onError(int code, String error) {
        // TODO: handle the callback result
    }

    @Override
    public void onSuccess(String data) {
        // TODO: handle the callback result
    }
}, uidList);
```

## Related Types

- [Base](/docs/chat/sdk/v4/android/models/base) (`Base`): Base used by this Android SDK type.
- [On Base](/docs/chat/sdk/v4/android/models/on-base) (`OnBase`): OnBase used by this Android SDK type.
