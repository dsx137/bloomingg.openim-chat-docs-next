---
title: 'Set advanced msg listener'
description: 'Explains the Android core binding method SetAdvancedMsgListener, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/event-handler/methods/set-advanced-msg-listener'
---

Use `SetAdvancedMsgListener` for set advanced msg listener in the OpenIM Android SDK.

## Feature

Registers or replaces the listener used to receive set advanced msg listener. Register listeners before login when you need login-time synchronization events.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                              |
| ---------------- | -------------------------------------------------------- |
| Method signature | `SetAdvancedMsgListener(listener OnAdvancedMsgListener)` |
| Capability       | Event listeners                                          |
| Source file      | `open_im_sdk/listener.go`                                |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void setAdvancedMsgListener(OnAdvanceMsgListener listener)
```

## Input Parameters

| Parameter  | Type                                                                                | Description              |
| ---------- | ----------------------------------------------------------------------------------- | ------------------------ |
| `listener` | [On Advance Msg Listener](/docs/chat/sdk/v4/android/models/on-advance-msg-listener) | Event listener instance. |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().messageManager.setAdvancedMsgListener(new OnAdvanceMsgListener() {
    @Override
    public void onRecvNewMessage(Message msg) {
        // TODO: handle the callback result
    }

    @Override
    public void onRecvC2CReadReceipt(List<C2CReadReceiptInfo> list) {
        // TODO: handle the callback result
    }

    @Override
    public void onRecvGroupMessageReadReceipt(GroupMessageReceipt groupMessageReceipt) {
        // TODO: handle the callback result
    }

    @Override
    public void onRecvMessageRevokedV2(RevokedInfo info) {
        // TODO: handle the callback result
    }
});
```

## Related Types

- [On Advanced Msg Listener](/docs/chat/sdk/v4/android/models/on-advanced-msg-listener) (`OnAdvancedMsgListener`): OnAdvancedMsgListener used by this Android SDK type.
- [C2 CRead Receipt Info](/docs/chat/sdk/v4/android/models/c2c-read-receipt-info) (`C2CReadReceiptInfo`): C2CReadReceiptInfo used by this Android SDK type.
- [On Advance Msg Listener](/docs/chat/sdk/v4/android/models/on-advance-msg-listener) (`OnAdvanceMsgListener`): OnAdvanceMsgListener used by this Android SDK type.
- [Group Message Receipt](/docs/chat/sdk/v4/android/models/group-message-receipt) (`GroupMessageReceipt`): GroupMessageReceipt used by this Android SDK type.
- [Revoked Info](/docs/chat/sdk/v4/android/models/revoked-info) (`RevokedInfo`): RevokedInfo used by this Android SDK type.
- [Message](/docs/chat/sdk/v4/android/models/message) (`Message`): Message used by this Android SDK type.
