---
title: 'Get users in group'
description: 'Explains the Android core binding method GetUsersInGroup, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/group/group-membership/get-users-in-group'
---

Use `GetUsersInGroup` for get users in group in the OpenIM Android SDK.

## Feature

Reads get users in group from the local SDK state or OpenIM service according to the method contract.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                                      |
| ---------------- | -------------------------------------------------------------------------------- |
| Method signature | `GetUsersInGroup(callback Base, operationID string, groupID, userIDList string)` |
| Capability       | Groups                                                                           |
| Source file      | `open_im_sdk/group.go`                                                           |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void getUsersInGroup(OnBase<List<String>> base, String groupID, List<String> userIdList)
```

## Input Parameters

| Parameter    | Type                   | Description                                     |
| ------------ | ---------------------- | ----------------------------------------------- |
| `base`       | `OnBase<List<String>>` | Async callback for success and failure results. |
| `groupID`    | String                 | Group ID.                                       |
| `userIdList` | `List<String>`         | List of user IDs, usually encoded as JSON.      |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().groupManager.getUsersInGroup(
    new OnBase<List<String>>() {
    @Override
    public void onError(int code, String error) {
        // TODO: handle the callback result
    }

    @Override
    public void onSuccess(List<String> data) {
        // TODO: handle the callback result
    }
},
    groupID,
    userIdList
);
```

## Related Types

- [Base](/docs/chat/sdk/v4/android/models/base) (`Base`): Base used by this Android SDK type.
- [On Base](/docs/chat/sdk/v4/android/models/on-base) (`OnBase`): OnBase used by this Android SDK type.
