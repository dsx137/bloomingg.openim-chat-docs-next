---
title: 'Get specified groups info'
description: 'Explains the Android core binding method GetSpecifiedGroupsInfo, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/group/managing-groups/get-specified-groups-info'
---

Use `GetSpecifiedGroupsInfo` for get specified groups info in the OpenIM Android SDK.

## Feature

Reads get specified groups info from the local SDK state or OpenIM service according to the method contract.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                                     |
| ---------------- | ------------------------------------------------------------------------------- |
| Method signature | `GetSpecifiedGroupsInfo(callback Base, operationID string, groupIDList string)` |
| Capability       | Groups                                                                          |
| Source file      | `open_im_sdk/group.go`                                                          |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void getGroupsInfo(OnBase<List<GroupInfo>> base, List<String> gidList)
```

## Input Parameters

| Parameter  | Type                                                                                                                     | Description                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------- |
| `callBack` | [On Base](/docs/chat/sdk/v4/android/models/on-base)`<List<`[Group Info](/docs/chat/sdk/v4/android/models/group-info)`>>` | Async callback for success and failure results.           |
| `gidList`  | `List<String>`                                                                                                           | Parameter passed according to the core binding signature. |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().groupManager.getGroupsInfo(new OnBase<List<GroupInfo>>() {
    @Override
    public void onError(int code, String error) {
        // TODO: handle the callback result
    }

    @Override
    public void onSuccess(List<GroupInfo> data) {
        // TODO: handle the callback result
    }
}, gidList);
```

## Related Types

- [Base](/docs/chat/sdk/v4/android/models/base) (`Base`): Base used by this Android SDK type.
- [On Base](/docs/chat/sdk/v4/android/models/on-base) (`OnBase`): OnBase used by this Android SDK type.
- [Group Info](/docs/chat/sdk/v4/android/models/group-info) (`GroupInfo`): GroupInfo used by this Android SDK type.
