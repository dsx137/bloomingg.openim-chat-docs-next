---
title: 'Logs'
description: 'Explains the Android core binding method Logs, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/android'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'android'
sourcePath: '/docs/chat/sdk/v4/android/logger/methods/logs'
---

Use `Logs` for logs in the OpenIM Android SDK.

## Feature

Logs provides the Android SDK operation for logs.

Initialize the SDK before calling user-scoped APIs, and pass a unique `operationID` whenever the core binding signature includes it so client and server logs can be correlated.

## Core Binding Method

| Item             | Description                                                                                                                 |
| ---------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Method signature | `Logs(callback Base, operationID string, logLevel int, file string, line int, msgs string, err string, keyAndValue string)` |
| Capability       | Logging                                                                                                                     |
| Source file      | `open_im_sdk/third.go`                                                                                                      |

## Android Call Reference

The Java method prototype in Android SDK v3.8.3 is shown below. The core binding signature explains the underlying `open_im_sdk` capability and parameter mapping.

```java showLineNumbers
public void logs(@LogLevel int logLevel, String currentClassName, int currentLineNum, @NonNull String msgStr, @Nullable String errStr, @Nullable String... extra)
```

## Input Parameters

| Parameter          | Type      | Description                                               |
| ------------------ | --------- | --------------------------------------------------------- |
| `logLevel`         | int       | Parameter passed according to the core binding signature. |
| `currentClassName` | String    | Parameter passed according to the core binding signature. |
| `currentLineNum`   | int       | Parameter passed according to the core binding signature. |
| `msgStr`           | String    | Parameter passed according to the core binding signature. |
| `errStr`           | String    | Parameter passed according to the core binding signature. |
| `extra`            | String... | Parameter passed according to the core binding signature. |



## Return Value

The result is returned through the callback. Success is delivered through `OnSuccess`, failure through `OnError`, and sending or upload methods may also report progress.

## Code Example

The following example uses the Android SDK v3.8.3 call style.

```java showLineNumbers
OpenIMClient.getInstance().logs(
    LogLevel.Info,
    "ChatActivity",
    120,
    "send message",
    null,
    "conversationID",
    conversationID
);
```

## Related Types

- [Base](/docs/chat/sdk/v4/android/models/base) (`Base`): Base used by this Android SDK type.
- [Log Level](/docs/chat/sdk/v4/android/models/log-level) (`LogLevel`): LogLevel used by this Android SDK type.
