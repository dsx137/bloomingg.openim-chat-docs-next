---
title: 'Uninitialize the SDK'
description: 'Explains the Uninitialize the SDK method in Flutter SDK 3.8.3+hotfix.12, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/application/methods/un-init-sdk'
---

Use **Uninitialize the SDK** when your application needs this OpenIM capability from Flutter SDK 3.8.3+hotfix.12. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `UnInitSDK` through the platform wrapper to uninitialize the sdk. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                     |
| ---------------- | ------------------------------- |
| Method signature | `UnInitSDK(operationID string)` |
| Capability       | Initialization and login        |
| Source location  | `open_im_sdk/init_login.go`     |

## Flutter Call Reference

Flutter SDK 3.8.3+hotfix.12 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`lib/src/manager/im_manager.dart`.

```dart showLineNumbers
void unInitSDK()
```

## Parameters

| Parameter | Type                 | Required   | Description                                 |
| --------- | -------------------- | ---------- | ------------------------------------------- |
| Parameter | See method signature | As defined | Pass according to the call signature above. |

## Return Value

Returns `void`. If the call fails, the Future throws an exception. Catch it in the application layer and keep the operationID in logs.

## Code Example

The following example shows the Flutter SDK 3.8.3+hotfix.12 call shape. Replace placeholder values with real business data and add production error handling.

```java showLineNumbers
OpenIMClient.getInstance().unInit();
```

## Related types

- See the surrounding SDK section for related pages.

## Related pages

- [Initialization and login](/docs/chat/sdk/v4/flutter/application/overview)
