---
title: 'Add to blacklist'
description: 'Explains the Add to blacklist method in Flutter SDK 3.8.3+hotfix.12, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/user/blacklist/methods/add-black'
---

Use **Add to blacklist** when your application needs this OpenIM capability from Flutter SDK 3.8.3+hotfix.12. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `AddBlack` through the platform wrapper to add to blacklist. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                                  |
| ---------------- | ---------------------------------------------------------------------------- |
| Method signature | `AddBlack(callback Base, operationID string, blackUserID string, ex string)` |
| Capability       | Blacklist                                                                    |
| Source location  | `open_im_sdk/relation.go`                                                    |

## Flutter Call Reference

Flutter SDK 3.8.3+hotfix.12 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`lib/src/manager/im_friendship_manager.dart`.

```dart showLineNumbers
Future<dynamic> addBlacklist({ required String userID, String? ex, String? operationID, })
```

## Parameters

| Parameter | Type    | Required | Description                  |
| --------- | ------- | -------- | ---------------------------- |
| userID    | String  | Yes      | User ID or list of user IDs. |
| ex        | String? | No       | Pass as defined.             |

## Return Value

Returns `Future<dynamic>`. If the call fails, the Future throws an exception. Catch it in the application layer and keep the operationID in logs.

## Code Example

The following example shows the Flutter SDK 3.8.3+hotfix.12 call shape. Replace placeholder values with real business data and add production error handling.

```dart showLineNumbers
final result = await OpenIM.iMManager.friendshipManager.addBlacklist(
  userID: 'userID',
  ex: 'ex',
);
```

## Related types

- [Base callback](/docs/chat/sdk/v4/flutter/models/base) (`Base`)
- [Friendship listener](/docs/chat/sdk/v4/flutter/models/on-friendship-listener) (`OnFriendshipListener`)

## Related pages

- [Blacklist](/docs/chat/sdk/v4/flutter/user/blacklist/overview)
