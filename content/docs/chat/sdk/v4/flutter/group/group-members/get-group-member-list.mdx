---
title: 'Get group member list'
description: 'Explains the Get group member list method in Flutter SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/flutter'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-08'
version: 'v4'
platform: 'flutter'
sourcePath: '/docs/chat/sdk/v4/flutter/group/group-members/get-group-member-list'
---

Use **Get group member list** when your application needs this OpenIM capability from Flutter SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying core binding.

## Overview

Call `GetGroupMemberList` through the platform wrapper to get group member list. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Core Binding Method

| Item             | Description                                                                                                      |
| ---------------- | ---------------------------------------------------------------------------------------------------------------- |
| Method signature | `GetGroupMemberList(callback Base, operationID string, groupID string, filter int32, offset int32, count int32)` |
| Capability       | Group                                                                                                            |
| Source location  | `open_im_sdk/group.go`                                                                                           |

## Flutter Call Reference

Flutter SDK v3.8.3 exposes the following platform method signature. The core binding signature above explains the underlying capability and parameter mapping.

Source:`lib/src/manager/im_group_manager.dart`.

```dart showLineNumbers
Future<List<GroupMembersInfo>> getGroupMemberList({ required String groupID, int filter = 0, int offset = 0, int count = 0, String? operationID, })
```

## Parameters

| Parameter | Type   | Required | Description                    |
| --------- | ------ | -------- | ------------------------------ |
| groupID   | String | Yes      | Group ID or list of group IDs. |
| filter    | int    | No       | Pass as defined.               |
| offset    | int    | No       | Pagination parameter.          |
| count     | int    | No       | Pagination parameter.          |

## Return Value

Returns `Future<List<GroupMembersInfo>>`. If the call fails, the Future throws an exception. Catch it in the application layer and keep the operationID in logs.

## Code Example

The following example shows the Flutter SDK v3.8.3 call shape. Replace placeholder values with real business data and add production error handling.

```dart showLineNumbers
final result = await OpenIM.iMManager.groupManager.getGroupMemberList(
  groupID: 'groupID',
  filter: 0,
  offset: 0,
  count: 0,
);
```

## Related types

- [Base callback](/docs/chat/sdk/v4/flutter/models/base) (`Base`)
- [GroupMemberFilter](/docs/chat/sdk/v4/flutter/models/group-member-filter) (`GroupMemberFilter`)
- [Group member info](/docs/chat/sdk/v4/flutter/models/group-members-info) (`GroupMembersInfo`)

## Related pages

- [Group](/docs/chat/sdk/v4/flutter/group/overview)
