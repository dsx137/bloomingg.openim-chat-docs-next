---
title: 'Get @all tag'
description: 'Explains the Get @all tag method in Electron SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/electron'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'electron'
sourcePath: '/docs/chat/sdk/v4/electron/message/creating-messages/get-at-all-tag'
---

Use **Get @all tag** when your application needs this OpenIM capability from Electron SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying OpenIM core.

## Overview

Call `GetAtAllTag` through the platform wrapper to get @all tag. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Underlying Capability

| Item             | Description                              |
| ---------------- | ---------------------------------------- |
| Method signature | `GetAtAllTag(operationID string) string` |
| Capability       | Conversations and messages               |
| Source location  | `open_im_sdk/conversation_msg.go`        |

## Electron Call Reference

Electron SDK v3.8.3 does not expose an independent `getAtAllTag` platform method. This page keeps the underlying capability notes without presenting another platform wrapper as the current Electron API.

## Parameters

No platform parameters are available because this SDK does not expose an independent method.

## Return Value

No platform return value is available because this SDK does not expose an independent method.

## Code Example

Electron SDK v3.8.3 does not expose an independent `getAtAllTag` method, so there is no platform code example for this page.

## Related types

- See the surrounding SDK section for related pages.

## Related pages

- [Conversations and messages](/docs/chat/sdk/v4/electron/message/overview)
