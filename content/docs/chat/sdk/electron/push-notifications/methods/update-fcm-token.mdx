---
title: 'Update FCM token'
description: 'Explains the Update FCM token method in Electron SDK v3.8.3, including purpose, parameters, return value, and usage boundaries.'
product: 'sdk'
context: 'chat/sdk/v4/electron'
template: 'guide'
status: 'draft'
lastUpdated: '2026-07-09'
version: 'v4'
platform: 'electron'
sourcePath: '/docs/chat/sdk/v4/electron/push-notifications/methods/update-fcm-token'
---

Use **Update FCM token** when your application needs this OpenIM capability from Electron SDK v3.8.3. This page follows the platform SDK reference and keeps the platform call signature visible next to the underlying OpenIM core.

## Overview

Call `UpdateFcmToken` through the platform wrapper to update fcm token. The current user usually needs to be initialized and logged in before calling business APIs. Keep `operationID` traceable so client and server logs can be matched during troubleshooting.

## Underlying Capability

| Item             | Description                                                                     |
| ---------------- | ------------------------------------------------------------------------------- |
| Method signature | `UpdateFcmToken(callback Base, operationID, fcmToken string, expireTime int64)` |
| Capability       | Push notifications                                                           |
| Source location  | `open_im_sdk/third.go`                                                          |

## Electron Call Reference

Electron SDK v3.8.3 does not expose an independent `updateFcmToken` platform method. This page keeps the underlying capability notes without presenting another platform wrapper as the current Electron API.

## Parameters

No platform parameters are available because this SDK does not expose an independent method.

## Return Value

No platform return value is available because this SDK does not expose an independent method.

## Code Example

Electron SDK v3.8.3 does not expose an independent `updateFcmToken` method, so there is no platform code example for this page.

## Related types

- [Base callback](/docs/chat/sdk/v4/electron/models/base) (`Base`)

## Related pages

- [Push notifications](/docs/chat/sdk/v4/electron/push-notifications/overview)
