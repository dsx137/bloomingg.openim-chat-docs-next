post:
  tags:
    - 'Conversations'
  summary: 'ConversationGetFullConversationIds'
  description: 'OpenIM Server POST route handled by c.GetFullOwnerConversationIDs. It accepts the JSON serialization of conversation.GetFullOwnerConversationIDsReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postConversationGetFullConversationIds
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of conversation.GetFullOwnerConversationIDsReq, matching the upstream wire type.'
          type: object
          additionalProperties: true
          properties:
            'idHash':
              type: integer
              format: int64
            'userID':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.conversation.GetFullOwnerConversationIDsReq'
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
                    properties:
                      'version':
                        type: integer
                        format: int64
                      'versionID':
                        type: string
                      'equal':
                        type: boolean
                      'conversationIDs':
                        type: array
                        items:
                          type: string
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.conversation.GetFullOwnerConversationIDsResp'
  security:
    - TokenAuth: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/conversation.go'
  x-source-gin-path: '/conversation/get_full_conversation_ids'
  x-source-request-type: 'conversation.GetFullOwnerConversationIDsReq'
  x-source-response-type: 'conversation.GetFullOwnerConversationIDsResp'
