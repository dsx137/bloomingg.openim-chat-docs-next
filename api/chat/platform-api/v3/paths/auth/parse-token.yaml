post:
  tags:
    - 'Authentication'
  summary: 'AuthParseToken'
  description: 'OpenIM Server POST route handled by a.ParseToken. It accepts the JSON serialization of auth.ParseTokenReq. Documented fields reflect the upstream wire types; handler-specific validation may impose additional constraints.'
  operationId: postAuthParseToken
  parameters:
    - $ref: ../../components/common/operation-id.yaml
  requestBody:
    required: true
    content:
      application/json:
        schema:
          description: 'JSON serialization of auth.ParseTokenReq, matching the upstream wire type.'
          type: object
          additionalProperties: true
          properties:
            'token':
              type: string
          x-source-schema-status: field-level
          x-source-schema-kind: protobuf
          x-source-protobuf-type: 'openim.auth.parseTokenReq'
  responses:
    '200':
      description: OpenIM result envelope. Inspect errCode; zero indicates success.
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/common/api-envelope.yaml
              - type: object
                properties:
                  data:
                    type: object
                    additionalProperties: true
                    properties:
                      'userID':
                        type: string
                      'platformID':
                        type: integer
                        format: int32
                      'expireTimeSeconds':
                        type: integer
                        format: int64
                    x-source-schema-status: field-level
                    x-source-schema-kind: protobuf
                    x-source-protobuf-type: 'openim.auth.parseTokenResp'
  security: []
  x-source-handler: 'https://github.com/openimsdk/open-im-server/blob/v3.8.3-patch.16/internal/api/auth.go'
  x-source-gin-path: '/auth/parse_token'
  x-source-request-type: 'auth.ParseTokenReq'
  x-source-response-type: 'auth.ParseTokenResp'
